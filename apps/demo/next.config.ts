import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  transpilePackages: ['@cbnsndwch/fractal-generator', '@cbnsndwch/fractal-react'],
};

export default nextConfig;
